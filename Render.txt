
# ── render.yaml ───────────────────────────────────────────────
# (guarda este bloque en un archivo separado llamado render.yaml)

services:
  - type: web
    name: drt-fichaje
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: JWT_SECRET
        generateValue: true          # Render genera uno aleatorio automáticamente
      - key: FIREBASE_CREDENTIALS
        sync: false                  # Lo pegas tú manualmente en el dashboard de Render